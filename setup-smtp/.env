# ==========================================
# CONFIGURATION SMTP - MAILHOG
# ==========================================
# Ce fichier sert de template pour la configuration
# Copiez ce fichier vers .env et personnalisez les valeurs si nécessaire
# Commande: cp env.example .env

# Nom du projet (utilisé pour les noms de containers et réseaux)
PROJECT_NAME=project-name

# Port SMTP pour l'envoi d'emails (port par défaut de MailHog)
SMTP_PORT=1025

# Port de l'interface web MailHog pour visualiser les emails
MAILHOG_UI_PORT=8025
